interface:
  display_name: "去 AI 味与声音守门"
  short_description: "发布前检查事实边界、AI 味和创作者声音。"
  default_prompt: "Use $creator-voice-guardian to run a final gate on this draft: [paste draft and source materials]."

policy:
  allow_implicit_invocation: true
