# candidates

这里存放雷达发现、但还没有确认上架的工作流候选卡。

命名建议：

```text
YYYY-MM-DD-候选工作流名称.md
```

候选卡来自 `../templates/workflow-candidate-card.md`。

状态说明：

- `raw`：刚发现，还没有整理。
- `candidate`：值得月亮判断。
- `hold`：有价值，但还不适合产品化。
- `pilot`：月亮确认后进入试点。
- `live`：验证稳定，可以进入公开工作台。
- `reject`：暂不引入。

